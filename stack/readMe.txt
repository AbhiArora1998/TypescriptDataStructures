stack datastructure

it is a linear data structure. Unline the array and linkedlist...  a stack only supports insertion and deletion from the top of the stack

ex = items from the top. LIFO (last in first out) (Pile of dishes)
it is also used in browser and editing history 

time complexisty
Push O(1)
pop O(1)
peek O(1)